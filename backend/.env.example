# Configuración del servidor
PORT=3000
NODE_ENV=development

# Base de datos MongoDB
MONGODB_URI=mongodb://localhost:27017/servilocal
# O usar MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://usuario:UjO4SOIyu0kd4Ey8@cluster.mongodb.net/servilocal

# JWT Secret (generar uno seguro en producción)
JWT_SECRET=tu_secreto_super_seguro_aqui_cambiar_en_produccion
JWT_EXPIRE=7d

# CORS
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:5500,http://localhost:5173

# Límites
MAX_FILE_SIZE=5242880
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

# Gemini API Key usada por el proxy del backend (NO exponer en el frontend)
GEMINI_API_KEY=

# Redis (opcional, para escalabilidad)
REDIS_URL=redis://localhost:6379
